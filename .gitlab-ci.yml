stages:
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"

# Build Docker image
build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build -t $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG -t $CI_REGISTRY_IMAGE:latest .
    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG
    - docker push $CI_REGISTRY_IMAGE:latest
  only:
    - main
    - master

# Deploy to DigitalOcean
# Deployment implementation pending - see OPS_DEPLOYMENT_REQUEST.md
deploy:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache curl openssh-client
  script:
    - |
      echo "Deploying HelloWorld app to DigitalOcean..."
      
      # Deployment method options (to be implemented by ops agent):
      # 1. DigitalOcean App Platform (webhook)
      # 2. DigitalOcean Droplet (SSH)
      # 3. DigitalOcean Kubernetes (kubectl)
      
      # Placeholder - deployment implementation needed
      echo "Deployment stage: Implementation pending"
      echo "See OPS_DEPLOYMENT_REQUEST.md for requirements"
  only:
    - main
    - master
  when: manual
  environment:
    name: production
    url: ${APP_URL:-https://your-app-url.com}

